<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 flex items-center flex-col justify-center">
        <!-- Styled Login Account Heading -->
          <!-- <div class="text-4xl font-bold text-white">
                <a href="/posts">NoteMate</a>
            </div> -->
        <h3 class="text-3xl font-semibold mb-6">Login Account</h3> <!-- Added text size, font weight, and bottom margin -->

        <!-- Login Form -->
        <form action="/login" method="post" class="w-full max-w-sm space-y-4">
            <input class="w-full px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="email" name="email" placeholder="email">
            <input class="w-full px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-800" type="password" name="password" placeholder="password">
            <input class="w-full px-4 py-3 rounded-md bg-blue-600 text-white cursor-pointer hover:bg-green-700 hover:scale-105 transition-all duration-300" type="submit" value="Login">
        </form>

        <!-- Link for Creating an Account -->
        <div class="mt-4">
            <p class="text-sm text-gray-400">Didn't have an account? 
                <a href="/createAccount" class="text-blue-400 hover:text-blue-600">Create one</a>
            </p>
        </div>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white">
    <div class="w-full min-h-screen p-10 relative">

        <!-- Top Navigation Bar -->
        <div class="absolute top-5 right-5 flex space-x-6 items-center text-2xl font-semibold text-white ml-auto">
            <!-- Home Link -->
            <a href="/" 
                class="text-gray-300 hover:text-white transition duration-300">
                Home
            </a>
            
            <!-- Posts Link -->
            <a href="/userPosts" 
                class="text-gray-300 hover:text-white transition duration-300">
                Your Posts
            </a>
        
            <!-- Smaller Logout Button -->
            <a href="/logout"
                class="px-3 py-1 rounded-md bg-red-600 hover:bg-red-700 transition-all duration-300 text-white text-base">
                Logout
            </a>
        </div>
        

        <!-- Welcome Message -->
        <h3 class="text-3xl mb-4">Hello <%= user.username %></h3>

        <!-- Post Form -->
        <form action="/post" method="post">
            <textarea
                placeholder="What's on your mind?"
                class="block mb-3 p-3 w-full sm:w-3/4 lg:w-1/2 resize-none bg-transparent border-2 border-zinc-800 rounded-md min-h-[6rem] max-h-[15rem] overflow-auto"
                name="content"
                oninput="this.style.height = 'auto'; this.style.height = this.scrollHeight + 'px';"
            ></textarea>
            <input
                type="submit" value="ShareScribe"
                class="px-4 py-3 mt-2 rounded-md bg-blue-600 hover:bg-green-700 hover:scale-105 transition-all duration-300 cursor-pointer">
        </form>
       
    </div>
</body>

</html>

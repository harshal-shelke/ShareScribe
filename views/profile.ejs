<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Hide the scrollbar globally */
        body, html {
            overflow-y: scroll; /* Ensures scrolling functionality remains */
            scrollbar-width: none; /* Firefox */
            -ms-overflow-style: none; /* Internet Explorer 10+ */
        }
        body::-webkit-scrollbar, html::-webkit-scrollbar {
            display: none; /* Chrome, Safari, Opera */
        }
        
        /* Hide scrollbar within textarea only when under max height */
        .scrollbar-hide {
            scrollbar-width: none; /* Firefox */
            -ms-overflow-style: none; /* IE and Edge */
        }
        .scrollbar-hide::-webkit-scrollbar {
            display: none; /* Chrome, Safari, Opera */
        }
    </style>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <a href="/logout"
            class="absolute top-5 right-5 px-4 py-2 rounded-md bg-red-600 text-white cursor-pointer hover:bg-red-700 transition-all duration-300">
            Logout
        </a>

        <h3 class="text-3xl mb-4">Hello <%= user.username %></h3>

        <form action="/post" method="post">
            <textarea
                placeholder="What's on your mind?"
                class="block mb-3 p-3 outline-none w-full sm:w-3/4 lg:w-1/2 resize-none bg-transparent border-2 border-zinc-800 rounded-md min-h-[6rem] max-h-[15rem] overflow-auto scrollbar-hide"
                name="content"
                oninput="autoResize(this)"
            ></textarea>
            <input
                class="block px-4 py-3 rounded-md bg-blue-600 text-white cursor-pointer hover:bg-green-700 hover:scale-105 transition-all duration-300"
                type="submit" value="Post Banger">
        </form>

        <script>
            function autoResize(textarea) {
                textarea.style.height = 'auto';
                textarea.style.height = Math.min(textarea.scrollHeight, 200) + 'px';  // 240px is approx 15rem
            }
        </script>

        <div class="mt-20">
            <h3 class="text-3xl mb-3">Your Posts</h3>
            <div class="post-container grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <% user.posts.reverse().forEach((post) => { %>
                    <div class="post p-5 border-2 mb-4 border-zinc-500 rounded-md transform transition-all duration-300 hover:scale-105 hover:shadow-md hover:shadow-zinc-900">
                        <h4 class="text-green-500 mb-2">@<%= user.username %></h4>
                        <p class="tracking-tight break-words">
                            <%= post.content %>
                        </p>
                        <div class="btns flex gap-4 mt-5">
                            <a class="text-red-500 flex items-center gap-1" href="/like/<%=post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                                <span class="text-gray-400 text-sm">(<%= post.likes.length %>)</span>
                            </a>
                            <a class="text-yellow-500" href="/edit/<%=post._id %>">Edit</a>
                            <a class="text-red-500" href="/delete/<%=post._id %>">Delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>

</html>

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Scribe</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">

        <!-- Header Navigation Links -->
        <div class="absolute top-5 right-5 flex space-x-6 items-center text-2xl font-semibold text-white ml-auto">
            <!-- Back to Posts Link -->
            <a href="/userPosts"
                class="text-gray-300 hover:text-white transition duration-300 text-xl">
                Back to Posts
            </a>
        
            <!-- Logout Button (Same as before) -->
            <a href="/logout"
                class="px-3 py-1 rounded-md bg-red-600 hover:bg-red-700 transition-all duration-300 text-white text-base">
                Logout
            </a>
        </div>
        

        <!-- Update Post Form -->
        <form action="/update/<%=post._id %>" method="post">
            <textarea placeholder="Update your Scribe..."
                class="block mb-3 p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%=post.content %></textarea>
            <input
                class="block px-4 py-3 rounded-md bg-yellow-600 text-black cursor-pointer hover:bg-green-700 hover:scale-105 transition-all duration-300"
                type="submit" value="Update Scribe">
        </form>

    </div>
</body>

</html>
